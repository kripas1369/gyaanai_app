import 'package:flutter_test/flutter_test.dart';

/// Full-app widget tests need plugin fakes (connectivity_plus, font loading, etc.).
/// Run `flutter run` on a device or use `integration_test` for end-to-end checks.
void main() {
  test('smoke', () => expect(1 + 1, 2));
}
